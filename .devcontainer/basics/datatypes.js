"use strict"; // It meanas that treat all JS Code as the newer version of the js .
              // It is not compulsory to write but it is a good practice .
              // As the old js doesnt have - classes , modules etc . But , the modern js has it .

              
// alert (3+3) // We are using the nodejs , not the browser . so this is not the proper syntax .

// CODE SHOULD BE HIGHLY READABLE = USE GOOD PRACTICES !

// TcE-39 is the original documentation . As it is tough so MDN is widely used ! 

let name ="Vivek"
let age = 20
let loggedIn = false
let state;

/*
Data types  : primitive
 1.] number => Its range is 2 to power of 53
2.] bigint => to store the value greater than int 
3.] string => ""
4.] null => standalone value (it is a object)
5.] undefined=> value dit hi nhi (it is a datatype)
6.] boolean => false/true
7.] symbol => uniqueness
*/

//typeof= is used to check the type of the variable data type 

console.log( typeof undefined);
console.log(typeof null);

